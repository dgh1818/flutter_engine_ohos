name: split_view
description: "Integration tests for the OHOS split view (parallel vision) feature."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.0-0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_driver:
    sdk: flutter
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
